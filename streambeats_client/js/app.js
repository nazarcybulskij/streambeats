define([
    'jquery',
    'underscore',
    'backbone'
], function($, _, Backbone) {
    var initialize = function() {
        console.log('App run ...');
    };

    return {
        initialize: initialize
    }
});
